# `DjangoReqResLog`

The DjangoReqResLog is a django middleware for logging request response details

## How to configure -
* Add logging configuration to django's settings.py if doesn't exist
* Add `request_id` under logging `filters`
